package main

import (
	"fmt"
	"math"
)

const name string = "afkcodes"

func main() {

	const n = 50000000000

	fmt.Println(name)
	const num = n / 50
	fmt.Println(num)

	fmt.Println(int64(num))

	fmt.Println(math.Sin(n))

}
